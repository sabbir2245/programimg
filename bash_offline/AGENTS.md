# AGENTS.md

## Overview

University bash offline tutorial / assignment. Minimal codebase — no build system, tests, or package manager.

## Python

- `add.py` has a working `add_nums(a, b)` function plus intentional garbage lines (12–14) at the end. Do not clean them up unless asked.
- `basic.sh.save` is a stub with a `#!/usr/bin/python3` shebang.

## Executing

```bash
python3 add.py
```

No test / lint / typecheck commands exist. Run manually and inspect output.

## PDFs

- `Bash_Offline_Tutorial.pdf` — tutorial material
- `Bash_Offline.pdf` — assignment description

Consult these for task requirements before editing code.
